#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../build/index.js" "$@"
else
  exec node  "$basedir/../../build/index.js" "$@"
fi
# cmd-shim-target=C:/Users/vitor/Desktop/Rancho - Sistema/node_modules/.pnpm/prisma@7.8.0_@types+react-d_aecdbbccffc2c6b34d5c6ae8337eba1a/node_modules/prisma/build/index.js
